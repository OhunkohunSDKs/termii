# CONFIGURATION

## Starting Local Server

Ensure you have a `.env` file at the root project folder which contains the following value(s).

```bash
IS_LOCAL_MACHINE="true"
```

## Example Variables

Some variables and constants used in the examples are loaded from a **`.env`** file. However, you can also define these values directly in your code without relying on the **`.env`** file.
